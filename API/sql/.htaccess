# Local setup only (schema + seed data with bcrypt hashes) -- not served to the app.
<IfModule mod_authz_core.c>
	Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
	Order deny,allow
	Deny from all
</IfModule>
